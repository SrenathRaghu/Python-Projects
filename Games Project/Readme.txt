# Initial List

actual_list = [' ','O',' ']

# Shuffling the list

shuffled_list = shuffle(actual_list)

# Get User's Guess

guess = user_guess()

# Check User's Guess

# based on the Guess of the User we will get the output!

user_guess_check(actual_list,guess)
